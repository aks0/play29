
var GameScore = function(player) { // later change it to team
	var
	player,
	points,
	redSets,
	blackSets;

	var getPoints = function() {
		return points;
	};

	var addPoints = function() {
		
	}
}